# Accessibility review rules

Target: **WCAG 2.2 AA**.

## What this file is for

Two automated layers already run on every PR, and they are the merge gate:

1. `eslint-plugin-jsx-a11y` (`npm run lint`) — static errors visible in the JSX source.
2. `axe-core` against Storybook stories in real Chromium (`npm run test-storybook`, via `@storybook/addon-a11y` + `@storybook/addon-vitest`; part of `npm test -- --run` in CI) — accessible names on the rendered DOM, ARIA validity, duplicate ids, target size.

**Do not restate what those two catch.** They are deterministic and they already failed the build if they found something. Your job is the half they cannot do: judging whether markup that *passes* the automated checks is actually usable. Everything below is a judgement call that requires reading the code, not running a tool.

## Out of scope: colour

Contrast is **not** reviewed here and `color-contrast` is switched off in the axe
run. The palette ships from the design system and consumers restyle it through
`theme`, so a contrast number measured in a story says nothing about what ends up
on a customer's page. Do not raise contrast findings; do not add contrast
exemptions to individual stories.

This does **not** exempt *state conveyed only by colour* (1.4.1) - that still
needs a non-visual equivalent such as `aria-pressed`.

## What to review

### Accessible-name *quality*

axe only checks a name exists. Check whether it is any good.

- Does `aria-label` describe the action, or does it restate the element (`aria-label='button'`, `aria-label='icon'`)?
- **Label in Name (2.5.3)**: if a control has visible text, the accessible name must contain that text. `<button aria-label='Submit'>Send</button>` is a failure — a voice-control user saying "click Send" cannot activate it.
- Does `alt` describe the image's purpose, or repeat a filename / the word "image"?
- Is the name stable, or does it interpolate something that can be empty at runtime? An `aria-label={someProp}` that resolves to `''` is a critical failure that only shows up for certain prop combinations. Check every branch.

### Decorative vs meaningful

- Inline SVG sitting next to visible text is decorative → needs `aria-hidden='true'` and `focusable='false'`.
- An icon-only control needs a real name, and its icon still needs `aria-hidden`.
- Flag `aria-hidden` on anything that is focusable — it creates an element a keyboard can reach but a screen reader cannot announce.

### State, not just role

- Toggles need `aria-pressed`; disclosures need `aria-expanded`; selected nav items need `aria-current`. A button whose *appearance* changes on click but whose *markup* does not is invisible to assistive technology.
- **1.4.1**: any state conveyed only by colour (icon fill, border tint) must also be conveyed non-visually.

### Focus intent

- Anything that opens (modal, drawer, popover) must move focus in, and **return focus to the trigger on close**. Check the close path, the Esc path, and the backdrop-click path — they often diverge.
- Native `<dialog>.showModal()` already traps focus. A hand-rolled focus trap on top of it is a bug, not a feature.
- Watch for elements hidden with a CSS class (`.invisible`, `opacity: 0`) rather than `display`/`visibility`/`hidden` — they stay in the tab order while being invisible.

### Live regions

- Errors that appear asynchronously need `role='alert'`; async content needs `aria-live` / `aria-busy`.
- Streamed content (this library renders AI-generated sections progressively) deserves special attention: the arrival of new sections should not steal focus, and loading states need a `role='status'` announcement.
- **Announce from a region that is already mounted, and put the message in the DOM as text.** A live region is announced from its *content*, so a region created together with its text, or one whose only text is an `aria-label`, is announced inconsistently across screen readers. `StatusRegion` is mounted with the root for exactly this reason: change its `message`, do not add a second `role='status'` inside a state that unmounts. A status region's accessible name is not computed from its content, so tests assert `toHaveTextContent`, not `getByRole('status', { name })`.
- **`aria-busy='true'` suppresses announcements from the region it is set on.** It belongs on a *container being replaced*, never on the `role='status'` element that carries the message.
- Skeletons and other placeholders are decorative once a real status region exists: give them `aria-hidden='true'` rather than a second live region.
- Adding `role='alert'` to an element already in the DOM announces nothing — alerts are announced on insertion. Force a remount (a changed `key`) when an existing node becomes an error.
- But a live region on a frequently re-rendering element is noise, not accessibility. Flag over-application too.

### Custom widgets

- `onClick` on a non-interactive element must also be keyboard-operable, or be a real `<button>`.
- Carousels, menus, tabs, comboboxes: check against the APG pattern for the expected keys, not just for "can you Tab to it".
- The `Carousel` and `ProductCard` primitives ship from `@constructor-io/constructorio-ui-components`; their internals are not fixable here, but how this repo *composes* them (wrapping cards in links/buttons, nesting interactive elements) is. What *can* be set from here: the carousel region's `aria-label`, the arrow buttons' translated `aria-label`, and the card's own name (`aria-label={product.itemName}` — a name computed from the card content would read the image alt and the title, i.e. the product name twice).

### i18n

An `aria-label` hardcoded in English bypasses the `translate()` system and is an accessibility bug in every other locale. All user-facing strings, including accessible names and live-region text, go through `translate()` with the key added to `defaultTranslations` (`src/utils/translate.ts`) and the `Translations` type (`src/types/index.ts`).

### Regression guard — the most important item

**Flag any diff that removes or weakens an existing accessibility affordance**: a deleted `aria-*` attribute, a semantic element replaced by a `div`, a removed `tabIndex`, a `role` dropped during a refactor, or a story's `a11y: { test: 'error' }` parameter downgraded to `'todo'`/`'off'`. This is the single most common way accessibility silently rots, and it is easy to miss when reviewing a large refactor for its stated purpose.

## Severity

Report accessibility findings as `**Important Issue**` or `**Suggestion**`. **Never** `**Critical Issue**` — the automated gate is lint + axe; this layer is advisory and must not read as a merge blocker.

If a finding is something lint or axe would already have caught, do not report it: the build would have been red.
